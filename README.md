## S3 Storage

- install docker on your machine
- copy `.env.example` as `.env` and write your username and password also api and console ports of storage
- run `docker-compose up -d`
- access console throgh your web browser `127.0.0.1:CONSOLE_PORT`

*** `ROOT_PASSWORD` most be at least 8 chracter ***